# AdMakers hire test(Next.js)

This the result of the hire test: I created  a responsive  page built with **Next.js (App Router)**, **TypeScript**, and **CSS** as per the instruction.  
I tried to make it as the figma design and I was able to get all the assets from figma.

---

##  Tech Stack

- Next.js (App Router)
- TypeScript
- CSS Modules
- Swiper.js (for sliders): npm install swiper

##  how to run the Project

```bash
npm install
npm run dev
